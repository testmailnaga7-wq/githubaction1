name: workflow2
on:
  push:
    branches:
       - main

jobs:
 myjob:
   runs-on: ubuntu-latest
   steps:
     - name: print message
       run: echo "Good Morning"
     - name: another message
       run: cat /etc/passwd
 newjob2:
    runs-on: windows-latest
    needs: 
       - myjob 
    steps:
      - name: message
        run: ipconfig
